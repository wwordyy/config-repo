eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka/

spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    
logging:
  level:
    org.springframework: INFO
